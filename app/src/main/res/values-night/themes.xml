<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Alfred" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/palette_blue</item>
        <item name="colorPrimaryVariant">@color/palette_blue</item>
        <item name="colorOnPrimary">@color/palette_foreground</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/palette_cyan</item>
        <item name="colorSecondaryVariant">@color/palette_cyan</item>
        <item name="colorOnSecondary">@color/palette_black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:navigationBarColor">@color/palette_background</item> <!-- Nav bar usually solid background in dark mode -->
        <item name="android:windowBackground">@color/window_background</item>
        <item name="colorSurface">@color/palette_background</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>
</resources>